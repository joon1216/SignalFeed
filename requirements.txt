# SignalFeed 의존성 (Session 44 정리 — 실제 import 기준)

# 수집
requests>=2.31.0
feedparser>=6.0.0

# 클러스터링
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
umap-learn>=0.5.0
hdbscan>=0.8.0

# 콘텐츠 생성 (Gemini 구조화 출력)
google-genai>=1.0.0
pydantic>=2.0.0

# 팩트 검증
yfinance>=0.2.0

# 카드 렌더링
playwright>=1.40.0
Pillow>=10.0.0

# Shorts 영상
moviepy>=2.1.0
gTTS>=2.5.0
matplotlib>=3.8.0
mplcyberpunk>=0.7.0

# 유틸리티
python-dotenv>=1.0.0
tqdm>=4.65.0
jsonlines>=4.0.0

# 테스트
pytest>=8.0.0

# 참고:
# - playwright 설치 후 `playwright install chromium` 필요
# - 제거된 의존성 (Session 44): torch, transformers, safetensors, langchain,
#   streamlit, plotly, fastapi, uvicorn, beautifulsoup4 (사용처 없음)
